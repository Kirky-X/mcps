# .github/workflows/publish-to-pypi-library_master.yml

name: Publish library_master to PyPI

on:
  push:
    tags:
      - 'library_master/v*'

jobs:
  build-and-publish:
    name: Build and publish library_master to PyPI
    runs-on: ubuntu-latest

    permissions:
      id-token: write
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install build dependencies
        run: python -m pip install --upgrade build

      - name: Build the package
        working-directory: ./library_master
        run: python -m build

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          files: library_master/dist/*

      - name: Publish package to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages_dir: library_master/dist/